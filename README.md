# FastAPI Swagger 예제

이 프로젝트는 FastAPI의 자동 생성 Swagger 문서를 확인하기 위한 간단한 예제입니다.

## 🚀 빠른 시작

### 1. 가상환경 설정
```bash
# 가상환경 생성
python -m venv venv

# 가상환경 활성화 (Windows)
venv\Scripts\activate

# 가상환경 활성화 (Linux/Mac)
source venv/bin/activate
```

### 2. 의존성 설치
```bash
pip install -r requirements.txt
```

### 3. 서버 실행
```bash
python main.py
```

또는

```bash
uvicorn main:app --reload
```

### 4. Swagger 문서 확인
서버가 실행되면 다음 URL에서 Swagger 문서를 확인할 수 있습니다:

- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc
- **API 루트**: http://localhost:8000/

## 📚 API 엔드포인트

### 기본 엔드포인트
- `GET /` - API 정보 및 문서 링크
- `GET /health` - 서버 상태 확인

### 사용자 관리
- `GET /users` - 모든 사용자 조회
- `GET /users/{user_id}` - 특정 사용자 조회
- `POST /users` - 새 사용자 생성
- `PUT /users/{user_id}` - 사용자 정보 수정
- `DELETE /users/{user_id}` - 사용자 삭제

### 검색 및 필터링
- `GET /users/search/` - 사용자 검색 (이름, 역할, 나이 범위)

### 통계
- `GET /stats` - 사용자 통계 정보

## 🔍 Swagger 문서 특징

이 예제에서 확인할 수 있는 Swagger 문서의 주요 특징들:

1. **자동 생성된 API 문서**: 모든 엔드포인트가 자동으로 문서화됨
2. **인터랙티브 테스트**: 브라우저에서 직접 API 테스트 가능
3. **Pydantic 모델 문서화**: 요청/응답 스키마 자동 생성
4. **Enum 타입 지원**: 사용자 역할 등의 열거형 타입 표시
5. **검증 규칙 표시**: 필드 길이, 범위 등의 제약 조건 표시
6. **태그별 그룹화**: 관련 엔드포인트들을 그룹으로 분류
7. **상세한 설명**: 각 엔드포인트와 필드에 대한 설명

## 🛠️ 사용된 기술

- **FastAPI**: 현대적이고 빠른 웹 프레임워크
- **Pydantic**: 데이터 검증 및 직렬화
- **Uvicorn**: ASGI 서버
- **Swagger UI**: 자동 생성된 API 문서 인터페이스

## 📝 주요 학습 포인트

1. **자동 문서화**: 코드만 작성하면 자동으로 API 문서가 생성됨
2. **타입 힌트 활용**: Python 타입 힌트를 통한 자동 검증
3. **Pydantic 모델**: 요청/응답 데이터 구조 정의
4. **HTTP 상태 코드**: 적절한 상태 코드 반환
5. **에러 처리**: HTTPException을 통한 에러 응답
6. **Enum 활용**: 제한된 값들의 타입 안전성
7. **선택적 필드**: Optional 타입을 통한 유연한 데이터 구조
